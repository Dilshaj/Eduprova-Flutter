import 'package:edupurva/constants.dart';
import 'package:flutter/material.dart';

class AppTheme {
  static const Color background = Color(0xFFFBFCFF);

  // Background Blobs Base Colors
  static const Color roseBlob = Color.fromRGBO(244, 63, 94, 0.1); // rose-500/10
  static const Color indigoBlob = Color.fromRGBO(
    99,
    102,
    241,
    0.1,
  ); // indigo-500/10
  static const Color blueBlob = Color.fromRGBO(
    59,
    130,
    246,
    0.1,
  ); // blue-500/10
  static const Color purpleBlob = Color.fromRGBO(
    168,
    85,
    247,
    0.1,
  ); // purple-500/10

  // Standard Gradient
  static const LinearGradient primaryGradient = kGradient;
}
